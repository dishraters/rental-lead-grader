export const metadata = {
  title: 'Rental Lead Grader',
  description: 'Zillow + AirDNA rental arbitrage lead scoring MVP'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
